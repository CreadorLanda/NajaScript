// Test do MathUtils com exports

// Importar o módulo MathUtils
import "MathUtils";

// Função principal para testar
fun main() {
    println("=== Teste do Módulo MathUtils ===");
    
    // Usar acesso via módulo
    println("MathUtils.add(5, 3) = " + MathUtils.add(5, 3));
    println("MathUtils.subtract(10, 4) = " + MathUtils.subtract(10, 4));
    println("MathUtils.multiply(6, 7) = " + MathUtils.multiply(6, 7));
    println("MathUtils.divide(20, 4) = " + MathUtils.divide(20, 4));
    
    // Constantes
    println("MathUtils.PI = " + MathUtils.PI);
    println("MathUtils.E = " + MathUtils.E);
    
    // Funções avançadas
    println("MathUtils.factorial(5) = " + MathUtils.factorial(5));
    println("MathUtils.power(2, 8) = " + MathUtils.power(2, 8));
    
    // Informações
    println("MathUtils info: " + MathUtils.info());
    
    println("\nTeste concluído com sucesso!");
}

// Executar a função principal
main(); 