// Teste simples do módulo NumeroUtil
import "NumeroUtil";

fun main() {
    println("=== Teste Simples do Módulo NumeroUtil ===");
    
    // Testar a função negativo
    int n = negativo(10);
    println("negativo(10) = " + n);
    
    // Testar verificação de negativos
    println("ehNegativo(n): " + ehNegativo(n));
    
    println("\n=== Teste concluído ===");
}

main(); 